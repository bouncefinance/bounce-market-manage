import { ExclamationCircleOutlined, StarOutlined } from '@ant-design/icons';
import { PageContainer } from '@ant-design/pro-layout';
import { Card, List, Image, Tag, Tabs, Input, Space, message, Modal, Button } from 'antd';
import React, { useEffect, useRef, useState } from 'react';
import { useRequest } from 'umi';
import request from 'umi-request';

import BounceLotteryNFTPro from '@/tools/web3/abi/BounceLotteryNFTPro'

const { TabPane } = Tabs;
const { Search } = Input;
const { confirm } = Modal;

const create = () => {
    
}

function createBox() {
    return (
        <>
            
        </>
    )
}

export default createBox
